# Copy to ~/.config/dotfiles/local.env.sh for machine-local environment values.
# Keep secrets and host-specific settings out of the tracked repo.
# export HOMEBREW_NO_AUTO_UPDATE=1
# export GOPATH="$HOME/.local/share/go"
