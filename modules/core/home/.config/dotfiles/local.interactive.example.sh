# Copy to ~/.config/dotfiles/local.interactive.sh for interactive helpers.
# Prefer new helper names over command-shadowing aliases.
# alias gs='git status --short --branch'
#
# Enable fzf shell keybindings/completion explicitly only if you want them:
# if command -v fzf >/dev/null 2>&1; then
#   [ -n "${ZSH_VERSION:-}" ] && eval "$(fzf --zsh)"
#   [ -n "${BASH_VERSION:-}" ] && eval "$(fzf --bash)"
# fi
