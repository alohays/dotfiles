# Standard SSH defaults; keep host secrets in ~/.ssh/config.local.
Include ~/.ssh/config.local

Host *
    ForwardAgent no
    HashKnownHosts yes
    ConnectTimeout 5
    ServerAliveInterval 60
    ServerAliveCountMax 3
